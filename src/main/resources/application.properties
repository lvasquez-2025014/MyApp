spring.application.name=kinalapp
server.port=${PORT:8302}

#Conexion a MySQL (Railway usa variables de entorno)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/dbClientess_in5am?createDatabaseIfNotExist=true}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Ludwing299}

#JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
